{
  "name": "@sigil-protocol/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Sigil agent wallets",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  }
}
